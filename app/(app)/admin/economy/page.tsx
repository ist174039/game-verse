import Link from 'next/link'
import { redirect } from 'next/navigation'
import { ArrowLeft, Landmark, LockKeyhole, ReceiptText, WalletCards } from 'lucide-react'
import { canAdmin, getAdminSession } from '@/lib/server/admin-auth'
import { AdminFreezeButton } from '@/components/admin/admin-freeze-button'

export const dynamic='force-dynamic'

export default async function AdminEconomyPage(){const session=await getAdminSession();if(!session)redirect('/dashboard');const[freezesQ,loansQ,liabilitiesQ,ledgerQ,listingsQ]=await Promise.all([session.serviceClient.from('economic_freeze').select('*').order('created_at',{ascending:false}).limit(100),session.serviceClient.from('club_loan').select('*').order('created_at',{ascending:false}).limit(100),session.serviceClient.from('club_liability').select('*').order('created_at',{ascending:false}).limit(100),session.serviceClient.from('ledger_transaction').select('*').order('created_at',{ascending:false}).limit(100),session.serviceClient.from('market_listing').select('id,status').eq('status','ACTIVE')]);for(const q of[freezesQ,loansQ,liabilitiesQ,ledgerQ,listingsQ])if(q.error)throw q.error;const freezes=freezesQ.data??[],active=freezes.filter((f:any)=>f.active),loans=loansQ.data??[],liabilities=liabilitiesQ.data??[];const canFreeze=canAdmin(session.role,'FREEZE');const outstandingLoan=loans.filter((l:any)=>['ACTIVE','DEFAULTED'].includes(l.state)).reduce((s:number,l:any)=>s+Number(l.outstanding_principal??0)+Number(l.outstanding_interest??0),0);const outstandingLiability=liabilities.filter((l:any)=>l.state==='OPEN').reduce((s:number,l:any)=>s+Number(l.outstanding_amount??0),0);return <div className="space-y-7"><section className="rounded-2xl border border-white/[0.07] bg-[#0b0b0b] p-5 sm:p-6"><Link href="/admin" className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground"><ArrowLeft className="h-3.5 w-3.5"/>Admin</Link><div className="mt-4 flex items-center gap-2"><Landmark className="h-5 w-5 text-primary"/><h1 className="text-2xl font-black">Economia global</h1></div><p className="mt-2 text-sm text-muted-foreground">Risco, dívida, liabilities, mercado e trilho contabilístico. Saldos nunca são editados diretamente.</p></section><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={LockKeyhole} label="Freezes ativos" value={active.length.toLocaleString('pt-PT')}/><Metric icon={WalletCards} label="Dívida ativa" value={`${outstandingLoan.toLocaleString('pt-PT')} S`}/><Metric icon={ReceiptText} label="Liabilities abertas" value={`${outstandingLiability.toLocaleString('pt-PT')} S`}/><Metric icon={Landmark} label="Listings ativos" value={String((listingsQ.data??[]).length)}/></section><section className="grid gap-5 xl:grid-cols-2"><Panel title="Freezes económicos"><div className="space-y-3">{freezes.length===0?<Empty text="Sem freezes."/>:freezes.map((f:any)=>{const target=f.user_id??f.club_id??f.universe_id;return <article key={f.id} className="rounded-xl border border-white/[0.07] bg-black/20 p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="font-black">{f.scope} · {target}</p><p className="mt-1 text-xs text-muted-foreground">{f.reason}</p><p className="mt-2 text-[10px] uppercase tracking-[.12em] text-muted-foreground">{f.active?'ATIVO':'LIBERTADO'} · {new Date(f.created_at).toLocaleString('pt-PT')}</p></div>{f.active&&canFreeze&&target&&<AdminFreezeButton scope={f.scope} targetId={target} freezeId={f.id}/>}</div></article>})}</div></Panel><Panel title="Ledger recente"><div className="divide-y divide-white/[0.06] border-y border-white/[0.06]">{(ledgerQ.data??[]).map((tx:any)=><div key={tx.id} className="py-3"><div className="flex items-center justify-between gap-4"><p className="text-sm font-bold">{tx.transaction_type}</p><p className="text-[10px] text-muted-foreground">{new Date(tx.created_at).toLocaleString('pt-PT')}</p></div><p className="mt-1 text-xs text-muted-foreground">{tx.reason??'Sem descrição'} · {tx.reference_type??'—'} {tx.reference_id??''}</p></div>)}</div></Panel></section></div>}
function Metric({icon:Icon,label,value}:{icon:typeof Landmark;label:string;value:string}){return <article className="rounded-2xl border border-white/[0.07] bg-[#0b0b0b] p-5"><Icon className="h-4 w-4 text-primary"/><p className="mt-3 text-xl font-black">{value}</p><p className="mt-1 text-xs text-muted-foreground">{label}</p></article>}
function Panel({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-2xl border border-white/[0.07] bg-[#0b0b0b] p-5 sm:p-6"><h2 className="text-xl font-black">{title}</h2><div className="mt-5">{children}</div></section>}
function Empty({text}:{text:string}){return <p className="py-10 text-center text-sm text-muted-foreground">{text}</p>}
