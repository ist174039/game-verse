import Link from 'next/link'
import { redirect } from 'next/navigation'
import { Banknote, Globe2, Landmark, LayoutDashboard, ReceiptText, Settings, ShieldAlert, Swords, Tickets, Users } from 'lucide-react'
import { canAdmin, getAdminSession } from '@/lib/server/admin-auth'

export const dynamic='force-dynamic'

export default async function AdminLayout({children}:{children:React.ReactNode}){const session=await getAdminSession();if(!session)redirect('/dashboard');const items=[{href:'/admin',label:'Overview',icon:LayoutDashboard,show:true},{href:'/admin/users',label:'Utilizadores',icon:Users,show:true},{href:'/admin/universes',label:'Universos',icon:Globe2,show:true},{href:'/admin/competition',label:'Competição',icon:Swords,show:true},{href:'/admin/economy',label:'Economia',icon:Landmark,show:true},{href:'/admin/payments',label:'Pagamentos',icon:Banknote,show:true},{href:'/admin/backoffice',label:'Backoffice',icon:Tickets,show:session.role!=='read_only_analyst'},{href:'/admin/config',label:'Config',icon:Settings,show:canAdmin(session.role,'CONFIG')},{href:'/admin/audit',label:'Audit',icon:ReceiptText,show:true}].filter(i=>i.show);return <div><div className="mb-6 overflow-x-auto rounded-2xl border border-white/[0.07] bg-[#0b0b0b] p-2"><nav className="flex min-w-max gap-1">{items.map(({href,label,icon:Icon})=><Link key={href} href={href} className="inline-flex h-9 items-center gap-2 rounded-xl px-3 text-xs font-semibold text-muted-foreground transition hover:bg-white/[0.04] hover:text-foreground"><Icon className="h-3.5 w-3.5"/>{label}</Link>)}</nav></div>{children}<div className="mt-8 flex items-center gap-2 rounded-xl border border-primary/10 bg-primary/[.02] px-4 py-3 text-[10px] uppercase tracking-[.12em] text-muted-foreground"><ShieldAlert className="h-3.5 w-3.5 text-primary"/>Admin protegido por role server-side · service key nunca é exposta ao cliente</div></div>}
